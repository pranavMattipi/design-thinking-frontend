import React from 'react';

const Footer = () => {
  return (
    <footer className="fixed bottom-4 left-4 z-50">
      <button className="w-8 h-8 rounded-full border-2 border-gray-400 flex items-center justify-center text-gray-500 hover:text-black hover:border-black transition-all">
        <span className="font-serif italic font-bold">i</span>
      </button>
    </footer>
  );
};

export default Footer;
